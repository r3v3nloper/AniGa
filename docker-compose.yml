services:
  aniga:
    build: .
    container_name: aniga
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - aniga-data:/data
    environment:
      - PORT=3000
      - DATA_DIR=/data
      - JWT_SECRET=${JWT_SECRET:-bitte-aendern-langer-zufaelliger-string}
      - TMDB_API_TOKEN=${TMDB_API_TOKEN:-}

volumes:
  aniga-data:
    driver: local
